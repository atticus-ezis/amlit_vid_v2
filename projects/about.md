# Summary

Create 'Project' obj. This is top-level reference for the whole pipeline. Responsible for create the 'Paths' which store the images

# Views

create_project:
user submits form with name
save obj
call obj function to create paths
return project id

# Model

Projects

# Functions

slugify
get_paths(defined in Objct)
